# SAATHI hub dependencies — single manifest (MASTER_ARCHITECTURE.md §4).
# Adding ANY dependency requires the justification protocol in §27.
# All pins chosen for win-arm64 wheel availability on the Snapdragon hub.

fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.2
paho-mqtt>=2.0
httpx>=0.27
onnxruntime>=1.18
# headless build per §4: VideoCapture works, no GUI deps (dashboard draws skeletons itself)
opencv-python-headless>=4.10
sounddevice>=0.4.6
rich>=13
pytest>=8.0
pytest-asyncio>=0.23
